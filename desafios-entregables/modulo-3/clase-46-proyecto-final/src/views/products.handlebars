<div>
    {{#if userDB}}
    <p>Hola <strong>{{this.first_name}}</strong>, estos son tus datos:</p>
    <br>
    <p>Nombre: <strong>{{this.first_name}}</strong></p>
    <p>Apellido: <strong>{{this.last_name}}</strong></p>
    <p>Edad: <strong>{{this.age}}</strong></p>
    <p>Email: <strong>{{this.email}}</strong></p>
    <p>Rol: <strong>{{this.role}}</strong></p>
    {{/if}}
    <br>
    <a href="/login">
        <button>Iniciar sesión</button>
    </a>
    <a href="/api/products/create">
        <button>Crear producto</button>
    </a>
    <a href="/api/session/logout">
        <button>Cerrar sesión</button>
    </a>
    <hr>
    <br>
    <h1>Productos</h1>
    <hr>
    <div>
        {{#each docs}}
        <li style="list-style: none;">
            <img src="{{this.thumbnail}}" alt="Imagen del producto">
            <h2>{{this.title}}</h2>
            <p>Descripción: {{this.description}}</p>
            <p>Precio: ${{this.price}}</p>
            <p>Stock: {{this.stock}}</p>
            <p>Categoría: {{this.category}}</p>
            <p>Código único: {{this.code}}</p>
            <button>Ver más</button>
            <button>Agregar al carrito</button>
            <br>
            <br>
            <br>
            <hr>
        </li>
        {{/each}}
    </div>

    <center>
        {{#if hasPrevPage}}
        <a href="/api/products?page={{prevPage}}">
            <button>Anterior</button>
        </a>
        {{/if}}
        <strong>Página {{this.page}} de {{this.totalPages}}</strong>
        {{#if hasNextPage}}
        <a href="/api/products?page={{nextPage}}">
            <button>Siguiente</button>
        </a>
        {{/if}}
    </center>
</div>